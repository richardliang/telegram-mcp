# Telegram API Credentials (Required - get from https://my.telegram.org/apps)
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef

# Session Management
TELEGRAM_SESSION_NAME=telegram_session
TELEGRAM_SESSION_STRING=1231231232erfdfdffd

# Remote MCP server settings
MCP_BIND_HOST=0.0.0.0
MCP_BIND_PORT=8000
MCP_PUBLIC_BASE_URL=https://telegram-mcp.example.com
# Optional on Railway if you use the generated Railway domain:
# RAILWAY_PUBLIC_DOMAIN=your-app.up.railway.app

# Single-user OAuth gate for Claude Desktop
MCP_AUTH_USERNAME=me
MCP_AUTH_PASSWORD=replace-this-now
MCP_AUTH_SCOPE=user
MCP_TOKEN_TTL_SECONDS=3600
# Set to 0 to issue non-expiring access tokens.

# Optional: colon-separated server-side file roots for file upload/download tools
MCP_ALLOWED_ROOTS=/srv/telegram-mcp:/tmp/telegram-mcp

# Optional: comma-separated host allowlist override for FastMCP transport security
MCP_ALLOWED_HOSTS=telegram-mcp.example.com,telegram-mcp.example.com:*

# Optional: defaults to false on public remote deployments
MCP_ENABLE_DNS_REBINDING_PROTECTION=false
